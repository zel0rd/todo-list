import styled from 'styled-components';

const RevisingContentsStyle = styled.textarea`
  font-weight: normal;
  font-size: 14px;
  line-height: 20px;

  width: 265px;
  height: auto;

  color: #010101;

  margin: 8px 0;

  overflow: auto;
  // resize: none;
`;

export default RevisingContentsStyle;
